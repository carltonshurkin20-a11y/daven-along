import { NextRequest, NextResponse } from "next/server"; import fs from "fs"; import path from "path"; export async function GET(_: NextRequest, context: any) { try { const { slug } = await context.params; const filePath = path.resolve(process.cwd(), "data", "torah", slug + ".json"); const data = fs.readFileSync(filePath, "utf-8"); return NextResponse.json(JSON.parse(data)); } catch(e: any) { return NextResponse.json({ error: "Parsha not found", detail: e.message }, { status: 404 }); } }